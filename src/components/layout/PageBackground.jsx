/**
 * Земля: борт транспортного ящика под лампой ночного дока.
 *
 * Один фиксированный слой вместо прежних шести (подложка, топография,
 * дрейфующие свечения, сетка, зерно, виньетка). Свечения и виньетка ушли
 * не ради экономии: в этом мире свет один и приходит сверху справа, а
 * глубина берётся плотностью маркировки, а не размытыми пятнами.
 *
 * Зерно доски лежит не здесь, а на самом полотне страницы (`.kns-world`
 * в styles.css): оно должно разрежаться к подвалу, а фиксированный слой
 * не знает, где у документа низ.
 *
 * Всё рисуется средствами CSS — ни одного загружаемого файла.
 */
export function PageBackground() {
  return <div className="page-bg" aria-hidden="true"></div>;
}
